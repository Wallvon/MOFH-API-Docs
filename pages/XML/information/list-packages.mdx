---
title: List packages
---

import { Callout } from 'nextra-theme-docs'
import { Tab, Tabs } from 'nextra-theme-docs'
import Link from 'next/link'

# List packages
This endpoint lists the packages available for the reseller.

<Callout type="error" emoji="❗️">
    This API endpoint is <u>**broken**</u>. Use the <Link href="/JSON/information/list-packages"><u>JSON endpoint</u></Link> instead.
</Callout>

## HTTP Endpoint

`GET https://panel.myownfreehost.net/xml-api/listpkgs.php`

## Usage

<Tabs items={['cURL', 'Python']}>
    <Tab>
        ```bash
        curl -u username:password "https://panel.myownfreehost.net/xml-api/listpkgs.php"
        ```
    </Tab>
    <Tab>
        ```python
        import requests
        from requests.auth import HTTPBasicAuth

        url = "https://panel.myownfreehost.net/xml-api/listpkgs.php"

        response = requests.get(url, auth=HTTPBasicAuth('username', 'password'))

        print(response.text)
        ```
    </Tab>
</Tabs>


## Response

<Callout emoji="💡">
    **The above request returns XML structured like the following:**

    ```xml
    ```
</Callout>
