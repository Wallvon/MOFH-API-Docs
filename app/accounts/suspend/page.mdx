---
title: Suspend account
description: Suspenda a vPanel user account
---

import { Callout, Tabs, Cards, Table } from "nextra/components";
import { XCircle, Key, CornerDownLeft, PlayCircle } from "react-feather";
import { JsonOnly, XmlOnly } from "@/components/ApiSwitcher";
import { GreenText } from "@/components/Text";

# Suspend account

This endpoint suspends the vPanel user account associated with the provided information.

## HTTP Endpoint

<br />
<JsonOnly>
  {/* prettier-ignore */}
  <code>
    ```console copy filename="URL"
    https://panel.myownfreehost.net/json-api/suspendacct.php
    ```
  </code>
</JsonOnly>
<XmlOnly>
  {/* prettier-ignore */}
  <code>
    ```console copy filename="URL"
    https://panel.myownfreehost.net/xml-api/suspendacct.php
    ```
  </code>
</XmlOnly>

## Parameters

<Callout type="important">
  `user` is the username you provided when creating the user account using
  [createacct.php](/accounts/create). This is <u>**not**</u> the vPanel username
  (hname_12345678).
</Callout>

<br />
<Table>
  <thead>
    <Table.Tr>
      <Table.Th>Parameter</Table.Th>
      <Table.Th>Required</Table.Th>
      <Table.Th>Type</Table.Th>
    </Table.Tr>
  </thead>
  <tbody>
    <Table.Tr>
      <Table.Td>`username`</Table.Td>
      <Table.Td>
        <GreenText>Yes</GreenText>
      </Table.Td>
      <Table.Td>string</Table.Td>
    </Table.Tr>
    <Table.Tr>
      <Table.Td>`reason`</Table.Td>
      <Table.Td>
        <GreenText>Yes</GreenText>
      </Table.Td>
      <Table.Td>string</Table.Td>
    </Table.Tr>
  </tbody>
</Table>

## Usage

<JsonOnly>
  <Tabs items={['cURL', 'Python']}>
      <Tabs.Tab>
          ```bash copy showLineNumbers filename="Shell"
          curl -X POST -u username:password -d "user=username&reason=My beautiful reason." "https://panel.myownfreehost.net/json-api/suspendacct.php"
          ```
      </Tabs.Tab>
      <Tabs.Tab>
            ```python copy showLineNumbers filename="Python"
            import requests
            from requests.auth import HTTPBasicAuth

            url = "https://panel.myownfreehost.net/json-api/suspendacct.php"
            data = {'user': 'username', 'reason': 'My beautiful reason.'} # For the username, use the same username you set when creating the account.

            response = requests.post(url, params=data, auth=HTTPBasicAuth('username', 'password'))

            print(response.json())
            # Do this if you only want to get the status code.
            print(response.json()["result"][0]["status"])
            ```
      </Tabs.Tab>

  </Tabs>
</JsonOnly>
<XmlOnly>
  <Tabs items={['cURL', 'Python']}>
    <Tabs.Tab>
            ```bash copy showLineNumbers filename="Shell"
            curl -X POST -u username:password -d "user=username&reason=My beautiful reason." "https://panel.myownfreehost.net/xml-api/suspendacct.php"
            ```
    </Tabs.Tab>
    <Tabs.Tab>
            ```python copy showLineNumbers filename="Python"
            import requests
            from requests.auth import HTTPBasicAuth
            from xml.etree.ElementTree import fromstring, ElementTree # Do this if you only want to get the status code.

            url = "https://panel.myownfreehost.net/xml-api/suspendacct.php"
            data = {'user': 'username', 'reason': 'My beautiful reason.'} # For the username, use the same username you set when creating the account.

            response = requests.post(url, params=data, auth=HTTPBasicAuth('username', 'password'))

            print(response.text)

            # Do this if you only want to get the status code.
            tree = ElementTree(fromstring(response.content))
            root = tree.getroot()
            for child in root:
                print(child[0].text) # The status code (can either be 0 or 1)
            ```
    </Tabs.Tab>

  </Tabs>
</XmlOnly>

## Response

<JsonOnly>
    ```json copy showLineNumbers filename="Response"
    {
        "result": [
            {
                "status": 1,
                "statusmsg": "Account username suspended"
            }
        ]
    }
    ```

</JsonOnly>
<XmlOnly>
    ```xml copy filename="Response"
    <suspendacct>
        <result>
            <status>1</status>
            <statusmsg>
                <script>if (self['clear_ui_status']) { clear_ui_status(); }</script>
                Changing Shell to /bin/false...Changing shell for username.
                Shell changed.
                Locking Password...Locking password for user username.
                marking user vhosts / databases for suspension.
                ..
                ..
                Completed, this account will be fully suspended in 2 minutes.
            </statusmsg>
        </result>
    </suspendacct>
    ```
</XmlOnly>

---

### Related pages:

<Cards>
  <Cards.Card
    icon={<PlayCircle />}
    title="Unsuspend account"
    href="/accounts/unsuspend"
  />
  <Cards.Card icon={<XCircle />} title="Errors" href="/errors" />
  <Cards.Card icon={<Key />} title="Authentication" href="/authentication" />
</Cards>

<br />

<Cards.Card
  icon={<CornerDownLeft />}
  title="Hosting account callbacks: account suspension"
  href="/callbacks/hostingAccount#account-suspension"
/>
